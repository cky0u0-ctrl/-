// 두수의 곱구하기 default 
export default function multi(x, y) {
    return x * y
}
// 누구 누구야 안녕~~ nemed
export function hello(name) {
    console.log(`${name}아 안녕~`)
}
//배열
export let fruits = ["오렌지", "배열", "사과"] 